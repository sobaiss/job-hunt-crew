{
  "name": "@job-hunt-crew/prisma",
  "version": "0.1.0",
  "private": true,
  "prisma": {
    "schema": "schema.prisma",
    "seed": "node prisma/seed.js"
  },
  "scripts": {
    "validate": "prisma validate",
    "migrate:dev": "prisma migrate dev",
    "generate": "prisma generate",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.3.1"
  },
  "devDependencies": {
    "prisma": "^6.3.1"
  }
}
